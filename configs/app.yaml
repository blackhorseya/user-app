app:
  name: "user-app"
  project: "sean-side"
  env: "local"
  signature: "aH9vh6ZCS7tfUd4q"

http:
  port: 8080
  mode: debug

db:
  url: "mongodb://user-app:changeme@localhost:27017/user-db?connect=direct&authSource=admin"

log:
  encoding: console
  level: debug
